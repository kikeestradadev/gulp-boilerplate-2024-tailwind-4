section(class="w-full max-w-[var(--main-container)] mx-auto px-[15px] border border-[steelblue]") 
	div(class="w-full max-w-[var(--container)] mx-auto border border-[tomato]") 
		ul
			-for(let i = 1; i<=5; i++)
				li lorem
					| #{i}
		ol
			-for(let i = 1; i<=5; i++)
				li lorem
					| #{i}